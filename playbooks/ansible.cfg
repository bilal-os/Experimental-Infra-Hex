[defaults]
inventory = inventory/hosts.yml # No need to pass -i flag in the commands
host_key_checking = False # Prevents prompts when connecting to new hosts
retry_files_enabled = False # Disable creation of retry files when a playbook fails
roles_path = roles # Directory where roles are stored