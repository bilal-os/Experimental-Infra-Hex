server {
    listen 80;
    server_name {{ item.domain }};

    root {{ item.path }};

    location /.well-known/acme-challenge/ {
        allow all;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
